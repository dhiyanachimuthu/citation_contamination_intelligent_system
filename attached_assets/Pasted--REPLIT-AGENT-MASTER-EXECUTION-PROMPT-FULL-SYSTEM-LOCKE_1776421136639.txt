🚨 REPLIT AGENT MASTER EXECUTION PROMPT (FULL SYSTEM LOCKED SPEC)

You are a senior backend engineer, data pipeline architect, and research-grade systems builder.

You are building a production-style MVP:

Citation Contamination Intelligence System

This system detects how retracted scientific papers propagate influence through real citation networks using ONLY verified datasets and APIs.

❗ ABSOLUTE EXECUTION LAW (OVERRIDES EVERYTHING)

You MUST strictly obey:

🚫 ZERO HALLUCINATION RULE (CRITICAL)

Under no circumstances are you allowed to:

generate fake papers
create fake DOIs
simulate citations
fabricate graph edges
invent metadata
assume missing API data exists
fill missing fields with guesses
create synthetic datasets
create placeholder nodes like “Paper A”

EVERY single output element MUST originate from:

✔ OpenCitations API
✔ Semantic Scholar API
✔ Retraction Watch dataset

If data does not exist:
→ output NULL or SKIP ONLY
→ NEVER substitute or approximate

🧠 CORE SYSTEM DEFINITION

You are building a fully working system that:

INPUT
DOI (primary input)
optional paper title (fallback ONLY if DOI fails)
OUTPUT

A complete real-data-driven analysis system that:

Detects retraction status
Builds citation graph (real only)
Expands 1–3 hops citations
Computes contamination propagation score
Ranks affected papers
Displays results in Streamlit dashboard
📦 SYSTEM ARCHITECTURE (DO NOT DEVIATE)

You MUST implement EXACTLY these modules:

1. DOI INPUT & VALIDATION MODULE
Responsibilities:
Accept DOI string
Validate format strictly using regex
Normalize DOI:
lowercase
remove prefixes like https://doi.org/
Reject invalid DOI immediately
RULE:

No processing if DOI invalid.

2. RETRACTION DETECTION MODULE (REAL DATA ONLY)
Data Source:

Retraction Watch dataset (CSV only)

Matching priority:
Exact DOI match
Fuzzy title match (ONLY if DOI missing)
Partial DOI match
OUTPUT FORMAT:
{
  "is_retracted": true/false,
  "reason": string | null,
  "year": number | null
}
STRICT RULE:

If not found → return:

{ "is_retracted": false }

NEVER return “unknown”.

3. CITATION FETCH ENGINE (REAL ONLY)
Data Source:

OpenCitations API ONLY

Behavior:

For each DOI:

fetch citing papers
return ONLY:
citing DOIs
metadata if available
HARD RULES:
no synthetic citations
no guessed links
no fallback generation
Failure handling:
retry with exponential backoff
if still failing → return EMPTY LIST ONLY
4. GRAPH BUILDER (NETWORKX – STRICT REAL GRAPH)
Graph Type:

Directed citation graph

Node Definition:

ONLY real DOIs from APIs or dataset

Edge Definition:

ONLY real citation relationships

Constraints:
max 150 nodes total
1–3 hop expansion only
must maintain visited set
must deduplicate DOIs
must normalize DOIs before insertion
STOP CONDITIONS:
node limit reached
no further real citations available
STRICT RULE:

Never create artificial nodes or edges to “fill graph”

5. METADATA ENRICHMENT MODULE
Data Source:

Semantic Scholar API ONLY

Fields:
title
abstract
citationCount
year
RULE:

If missing field → MUST be NULL
DO NOT infer or guess content

6. CONTAMINATION RISK ENGINE (RULE-BASED ONLY)

NO ML. NO HEURISTICS BEYOND SPEC.

FORMULA:
depth_weight =
  level 1 → 1.0
  level 2 → 0.5
  level 3 → 0.2

influence = log(1 + citation_count)

risk_score = depth_weight × influence
RETRACTED PAPER RULE:

If paper is retracted:
→ risk_score × 2.0

7. HIGH-RISK CLASSIFICATION MODULE

Keyword-based ONLY:

If title/abstract contains:

"systematic review"
"meta-analysis"
"review"

Then:

assign HIGH RISK FLAG

NO semantic reasoning allowed.

8. RANKING ENGINE

Sort papers strictly by:

risk_score (descending)
citation_count
graph_depth

Must be deterministic (same input → same output always)

9. STREAMLIT DASHBOARD (REAL DATA VISUALIZATION ONLY)
UI Components:
DOI input box
Run analysis button
Retraction status panel
Results table (ONLY real data):

Columns:

DOI
title (or NULL)
citation_count (or NULL)
depth_level
risk_score
GRAPH VISUALIZATION:

Use NetworkX / PyVis

Nodes:

ONLY real DOIs

Edges:

ONLY real citations

Node colors:

red → retracted
orange → high risk
yellow → medium risk
green → low risk

If no data:
→ show “No real citation data available from APIs”

🌳 GRAPH SAFETY SYSTEM

You MUST enforce:

no infinite loops
strict visited tracking
hard cap at 150 nodes
removal of invalid nodes
proper handling of disconnected components
no graph padding or fake completion
⚠️ EDGE CASE HANDLING (MANDATORY)
API Failures:
retry with exponential backoff
final fallback = empty result ONLY
Missing Citations:
skip node safely
Broken DOI:
exclude immediately
Sparse Graph:
return partial real graph
Duplicate DOIs:
deduplicate strictly using canonical form
Self-Citations:
detect and downweight influence
❌ ABSOLUTE FORBIDDEN ACTIONS

You must NEVER:

fabricate papers
simulate API responses
generate fake graphs
invent citations
hallucinate metadata
create synthetic datasets
guess missing values
“complete” missing graph structure
🧠 FINAL SUCCESS CONDITION

System is valid ONLY IF:

✔ every node is real
✔ every edge is API-derived
✔ no synthetic data exists
✔ graph is reproducible
✔ retraction data is real
✔ metadata is real or NULL
✔ system gracefully degrades on missing data
✔ full pipeline works end-to-end

🚀 EXECUTION BEHAVIOR DIRECTIVE

You must operate with:

maximum engineering rigor, strict adherence to data truth, deep implementation completeness, and no deviation from real-world data sources.

You are NOT allowed to simplify by inventing missing logic.

You must implement ONLY what can be supported by real data.